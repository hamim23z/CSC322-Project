"use client"; // 👈 Required because this page uses hooks

import AdminRejectQueue from "../components/AdminRejectQueue";

export default function AdminRejectPage() {
  return <AdminRejectQueue />;
}
